server:
  port: 35000
spring:
  application:
    name: springcloud-eureka
  profiles:
    active: dev


---
spring:
  profiles: dev
eureka:
  instance:
    hostname: localhost
    preferIpAddress: true
    instanceId: eureka.talelife.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${spring.security.user.name}:${spring.security.user.password}@localhost:35000/eureka 
---
spring:
  profiles: prod